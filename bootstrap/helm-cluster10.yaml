apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: capi-cluster10
  namespace: default
spec:
  interval: 1m
  chart:
    spec:
      chart: ./charts/capi-vsphere
      sourceRef:
        kind: GitRepository
        name: capi-vsphere
        namespace: flux-system
      interval: 1m
      valuesFiles:
        - charts/capi-vsphere/values-cluster10.yaml 
